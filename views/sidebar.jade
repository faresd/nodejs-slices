div.search
    div.search-icon
        i.fa.fa-search
    form(method="get", action="/search")
        input(type = "text", placeholder = "search", name = "q")
div.sidebar-section
    h3.blog-nav-item
- var children = helpers.pages.getHomeChildren(home)
    div.sidebar-section
        h3 !{helpers.pages.pageLink(home, helpers.pageUrl(doc))}
        ul
            each child in children
                li !{helpers.pages.pageLink(child)}

div.sidebar-section
    h3 Archives
    ul
        li
            a